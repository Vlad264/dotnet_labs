using System;

namespace LineCounting
{
    class LineCounting
    {
        private readonly string format;

        public LineCounting(string format)
        {
            this.format = format;
        }

        public int Count()
        {
            return 0;
        }
    }
}